server:
  port: 8080
spring:
  application:
    name: consul-consumer
  cloud:
    consul:
      host: 192.168.2.104
      port: 8500
      discovery:
        heartbeat:
          enabled: true
        instance-id: ${spring.application.name}